---
name: 2026-05-10 https://www.youtube.com/watch?v=ZQKMZIP3Fzg
keywords: (placeholder)
metadata:
  url: https://www.youtube.com/watch?v=ZQKMZIP3Fzg
  source: SOURCE_TYPE_YOUTUBE_VIDEO
  date: 2026-05-16T14:22:32.566Z
  notebook: 1.2. FinOps & Cloud Economics
---
hello everyone welcome to one more lecture of our course on material Union systems and today we continue talking about GPU programming now we are going to focus on the GPU memory hierarchy there's different memory spaces that threads can use when programming a GPU um we are going to go into the details very quickly but first of all let me very briefly recap on what we covered about GPU programming or previous lecture let me remind you as well that the reference book for this course is the boot programming programming massively para processors remember that when programming a GPU or at the regime system with CPUs and gpus we are typically going to use CPU threads that will run on the CPU and GPU kernels CPU threads will execute sequential or modestly parallel sections that will run on the Jeep on the CPU course while massively parallel sections of code will run on the GPU so we will normally start the execution on the host or the CPU running some serial code or modestly parallel code by using one single thread or just a few threads and at some point we will offload computation to the GPU by calling launching and GPU kernel remember that when launching a GPU current we need to First Define what CS execution configuration typically the number of blocks per grid so the total number of blocks and the number of threads per block when the X on the GPU when the kernel terminates the control returns to the cost that may execute some more serial or modestly parallel code and eventually we may launch a different kernel or maybe the same one in iterative algorithms remember that our first example was the vector addition this is the code for the host of the vector addition where we first allocate GPU memory then we copy data from the CPU memory to the GPU memory then we perform computation on the GPU we first Define the execution configuration here is the number of threads per block 512 in this example and the total number of blocks and we launched the current when the execution of the kernels terminates we copy results from the GPU memory to the CPU memory you know from the device to host and we can finish your program by freeing and or deallocating any memory array that was allocated in GPU memory so remember the software the GPU software hierarchy consists of threads at the lower level these threads are grouped into thread blocks that are our map onto the different GPU cores and the total collection of thread blocks represents the grid uh there is a some novelty in the most recent architecture of Nvidia that is the h100 that was announced basically one year ago and um uh in between the uh the thread block and the grid um the new uh h100 architecture defines the thread block clusters so now instead of having agreed composed by thread blocks now we can have a grid composed by thread block clusters and these thread block clusters are composed at the same time by uh Threat by thread blocks so now the hierarchies thread at the lower level then we have thread blocks then we have thread block clusters and finally green and the interesting thing of the thread block clusters is that the the threads inside the thread blocks inside the thread block cluster can directly communicate with with a new network that the um these h100 architecture will introduce after these um Refresh on the GPU software hierarchy let's go and talk about the GPU memories remember the floor plan of uh any Nvidia gpus in particular this one is the h100 remember as well that here we observe the GPU cores inside the GPU cores there are different memory spaces like registers L1 or the scratch Pad memory that is called share memory and video terminology this uh the GPU cores or streaming multiprocessor processors or SM as they are called via video have access to the L2 cache and then they also have access to the externality of cheap memory in the h100 will be hvn3 memory and as you can see there will be up to three six Stacks three on each size of hvn3 memory accessed by the corresponding memory controllers that we can first check a more simplified representation of the memories in the GPU architecture each of these SMS or GPU cores have some control hardware and they have the um Cindy units in the cells where we are going to execute instructions from different warps and from these small Cindy Lanes we can access registers each individual thread will have access to its own registers and the access to these registers is super fast basically one cycle but threads can also access data in the L1 data cache or the nearby shared memory then the share memory is a scratch Pad memory it's a software managed cache that can be used to share data between threads belonging to the same thread block the access to both share memory and Maryland cash takes more or less the same amount of Cycles we could say something like five cycles and similar latency we would also have to access the constant cache which is another cache for constant data if we go with cheap we need to go off cheap to read from the of cheap drum that is called the global memory now the latency will be hundreds of Cycles for example around 500 cycles and in between the global memory and the SMS or the GPU cores we have the L2 cache as Novelties in the h100 GPU we will see the possibility of doing direct copy from Global memory to the share memory be without having to go through the um uh the the the lanes in the GPU cores and the registers and there will also be possible to communicate directly from SM to SM inside the same thread block cluster uh in this h100 GPU will have a pretty large L2 of about 50 megabytes and also pretty large Global memory hvn3 memory of 80 gigabytes and also um very interesting Global memory bandwidth of three terabytes per second that will be basically twice as high as the bandwidth of the previous generation the A1 100 GPU there are not so many details so far about these new architecture but hopefully in later editions of this course we will tell you more and we will even have some experience with these different features such as the direct copy or the sm-2sm communication but now we know that there are different memory spaces registers constant cash share memory lwink hash L2 cache Global memory and there are different ways of using them in our program we will use qualifiers to Define where one specific value or one specific array is allocated automatic variables will go directly to registers they will be allocated in registers by the compiler and if we Define inside the GPU kernel we Define an array like this it will be a private array for each individual thread and the compiler if possible will allocate it in registers as well if not if there are not enough the registers maybe because this n is too high then it will have to be allocated in the off chip theorem when we want to allocate something in the shared memory we will use this shared qualifier here and if we want it to be in the constant memory for it access its access from the constant cache we will use this constant qualifier if we want to allocate something in global memory we can use this device qualifier and these devices optional we use with shared and constantly so from the point of view of a GPU thread running on a GPU core it has access to its own registers that can use privately threads that belong to the same thread block can access the same data in shared memory and threads in different thread blocks can access the same data or different data from the external of Chip memory that is called the global memory and also from the constant memory that is allocated also in the of Chip memory but can be cached inside each SM as we have seen in a previous slide so how do are we going to use these different memory spaces for now we are going to talk about the shared memory and we are going to see how different threads can share data allocated in this shared memory and can communicate using this shared memory the shared memory is going to be especially useful when we have data reuse inside the same thread block for example if we have to apply one filter to one image and we assign one pixel of the image to each thread in the GPU when applying the filter what we normally do is reading pixels that surround the specific pixel that we are focused on and we do some calculation using these nearby pixels but one thing that you can observe here is that if this pixel is assigned to one thread and these other pixel is assigned to another thread in the same thread block they they are going to share the some values that they both will need to read this is what we call data reuse so these six pixels here will be used by both This Thread operating here and this thread operating here so this is what we call the data reuse uh in the most straightforward implementation of this code we would do just something like this this is like the the code here corresponds to a gaussian filtering where we are applying we are applying one um filter these Gauss here uh on top of each pixel of the image right and and and and and and these two for Loops allow us to go over all nine elements uh that are under the uh filter the Gauss filter that we are applying to every pixel in the image but as I said here there is a lot of data reused because these six elements or D60 excels will be assigned will be used by two different threads so what we can do here is take advantage of these data reels and uh use one technique that is called tiling sometimes also called blocking that consists of defining tiles of data that are going to be reused by many different workers or many different threads in the particular case of the GPU what we are going to do is place in this style into the shared memory because the shared memory can be accessed by all the threads belonging to the same thread block so that's what we do we Define some space in shared memory then we load the entire tile from the global memory of Chip memory into the shared memory and then threads will simply access the pixels that they need for their own computation for applying the filter they will access these pixels from these array L data that is in shared memory observe as well that between loading the title into share memory and the actual computation we need to use these scene threads instruction here or sync threads function here that allows us to synchronize all threads in a block this is a way of avoiding hazards like read after right right after read or right after right when accessing shared memory or Global memory um from threads inside the same block the same treadmill and it's important to use because thread blocks will typically have more than 32 threads that are that is the number of threads that belong to the same word is 32. if we have 64 threads for example in our thread block that means that we have two worms and now remember that the execution on the GPU core pipeline is by issuing instructions from different verbs so what that means is that instructions form different works even if they are the same instruction are executing at different times so if we have two warps loading data from the global memory to the shared memory we need to make sure that all the desired data is loaded the entire tile is loaded before we start using the data in that tile so that's why after loading we will use these sync threads exactly as we did in this example code here so in order to better understand how to take advantage of data we use we are going to go with a very important kernel that is matrix multiplication in matrix multiplication uh what we do is compare performing the multiplication row by column um of two matrices amb and we'll obtain one output Matrix C this is our Matrix C of size M by n in this simple example this is our Matrix V and a and this is our Matrix V so when we want to compute one individual pixel we will we'll have to read an entire row of a and an entire column of e of B the most straightforward parallelization approach is to assign one thread to each element of the output Matrix C remember that in the GPU these threads are grouped into thread blocks so we are already partitioning The Matrix the output Matrix C into several tiles several parts that are assigned to different thread blocks so all these pixels here will be computed by one thread block let's call it thread block zero zero these pixels here will be these outputs of element of C will be computed by the next red block and so on so forth so one thread this specific thread here will read these entire row and this entire column will perform the dot product operation of these two and accumulate results finally in these output element of C so this is the actual code uh because the Matrix is a two-dimensional organization we are using two-dimensional blocks remember that we talked about them in the previous lecture so by using the block index block dimensions and thread indices we can calculate the row and the column for each element of the Matrix C and then we access the entire row of a and the entire column of B in order to multiply and accumulate and obtain some final result that will be stored in the corresponding Row one column of Matrix C but the observation here is that there is a lot of reuse why is that because if we have four threads Computing these four elements of C here all these four threads need to access all values all elements of these specific row of Matrix a and if we think about these four elements and the corresponding four threads all the four threads will have to access the same column of Matrix V so we can take advantage of these data reels by doing a tile matrix multiplication in the tile matrix multiplication we divide the input matrices A and B into tiles and step by step we are going to load tiles into the shared memory ending use the data in share memory to perform the computation faster because accessing share memory is you know is significant can be faster basically two orders of magnitude faster than accessing Global memory so the threats of this the thread block that is in charge of this style of C will go to memory will read uh the tile they will read the title we will place them into share memory and then they will perform the partial matrix multiplication to obtain these values here and then we continue repeating the same thing for the next style and then for the next style and this way we'll obtain the final results for these elements of Matrix C so this is the code of the matrix multiplication as you can see we first declare arrays in shared memory and one tile of a and one tile of B will be placed there um we have these indices for row and column we initialize the accumulator for each individual thread and then for each of the tiles we first have the threads go to the global memory read the corresponding type from a the corresponding tile from B and placing them into the share memory and we need to synchronize because remember we are running lightly thread blocks with multiple warps so we need to synchronize in order to make sure that all the data of the tile is in the share memory and then we can compute using the tiles in share memory accumulating in this sum variable for each element of the output Matrix and at some point we will write the results when we go through all the tiles we will write the result in them uh in the final Matrix observe as well that we need to have these same threads also after these inner inner for Loop because uh threats will have to wait uh in order to make sure that all the data in the tile has been used as much as needed before replacing that data with the contents of the next Style okay this is all for today you can read more in the book uh programming music parallel processors in chapter five but I would also recommend you to watch the long version of this lecture where you can find more details about the GPU memory hierarchy about how to perform a basic tile Matrix Matrix multiplication and also a few more details about the new h100 architecture this is all for today I really appreciate your attention and I hope to see you in the next lecture 
